#include <stdio.h>

int main()
{

    // While Loop
    int i = 1;      // initilization
    while (i <= 10) // condition
    {
        printf("%d\n", i);
        i++; // increment
    }

    return 0;
}