#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    scanf("%d", &n);
    int star = n;
    int space = 1;

    for (int i = 1; i <= n; i++)
    {
        for (int j = 1; j < n; j++) {
            
        }

        printf("\n");
    }
    return 0;
}