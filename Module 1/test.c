#include <stdio.h>
int main()
{
    // printf() displays the string inside quotation
    printf("Hello, Rabbi Hasan!");

    // int a;
    // scanf("%d", &a);
    // printf("a = %d", a);
    return 0;
}
